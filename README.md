# Lucky Animal Studio

中古奇幻羊皮紙風格的 Astro 單頁式 Bento Grid 網站。

## 內容維護

- 編輯 `src/data/cards.json` 可更新卡片內容、排序與尺寸。
- 以同名檔案覆蓋 `public/images/announcements/` 內的公告圖即可換圖。
- 卡片資料型別統一定義於 `src/data/schema.ts`。

## 本機開發

```sh
npm install
npm run dev
```

正式建置使用 `npm run build`。
